<!doctype html>
<html lang="{{ request.locale.iso_code }}">
<head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Gift card — {{ shop.name }}</title>
{{ 'base.css' | asset_url | stylesheet_tag }}</head>
<body><main class="fe-page fe-page--narrow fe-section">
  <header style="text-align: center;"><span class="fe-eyebrow">{{ shop.name }}</span><h1 class="fe-display">Gift card</h1></header>
  <div style="border: 1px solid var(--fe-ink); padding: var(--fe-space-7); margin-top: var(--fe-space-6); text-align: center;">
    <span class="fe-eyebrow">Value</span>
    <p class="fe-display" style="font-size: 4rem; color: var(--fe-accent);">{{ gift_card.balance | money_without_trailing_zeros }}</p>
    <p style="margin-top: 1rem;"><strong>Code</strong><br><code style="background: var(--fe-paper-2); padding: 0.5rem 1rem; letter-spacing: 0.08em; font-family: monospace;">{{ gift_card.code | format_code }}</code></p>
    {%- if gift_card.expires_on -%}<small>Expires {{ gift_card.expires_on | date: '%B %-d, %Y' }}</small>{%- endif -%}
    <p style="margin-top: 1.5rem;"><a class="fe-btn" href="{{ shop.url }}">Choose a book</a></p>
  </div>
</main></body></html>
